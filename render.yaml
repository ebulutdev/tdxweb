services:
  - type: web
    name: tdxweb
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd chatbot && uvicorn api_chatbot:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11 